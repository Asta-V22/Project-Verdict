# Dev/test dependencies
# Install: pip install -r requirements-dev.txt
# (This includes all production dependencies via -r)

-r requirements.txt

# Testing
pytest>=8.0.0
httpx>=0.27.0

# Linting & formatting
ruff>=0.8.0

# Type checking (configured but not strictly enforced)
mypy>=1.13.0
